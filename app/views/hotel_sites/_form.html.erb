<%= form_with(model: hotel_site, local: true) do |form| %>
  <% if hotel_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotel_site.errors.count, "error") %> prohibited this hotel_site from being saved:</h2>

      <ul>
        <% hotel_site.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hotel_id %>
    <%= form.text_field :hotel_id %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.text_field :site_id %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
